//All components i.e. functions must start with Capital. and must have .jsx extension (especially when using Vite)
function Chai(){
    return (
        <h1>Learning react from ChaiAurCode | Anam Saeed</h1>
    )
}

export default Chai;